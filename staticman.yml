comments:
  allowedFields: ["name", "email", "content"]
  branch: "master"
  commitMessage: "Added Staticman comment data"
  filename: "comment-{@timestamp}"
  format: "yaml"
  moderation: true
  name: "Julian I. Kamil / etechcetera"
  path: "_data/comments/{options.slug}"
  requiredFields: ["name", "email", "content"]
  transforms:
    email: md5
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
