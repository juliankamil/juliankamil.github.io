comments:
  allowedFields: ["name", "email", "message"]
  branch: "master"
  commitMessage: "Added Staticman comment data"
  filename: "entry{@timestamp}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
  moderation: true
  name: "juliankamil.github.io"
  path: "_data/comments/{options.slug}"
  requiredFields: ["name", "email", "message"]
  transforms:
    email: md5
