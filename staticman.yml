comments:
  allowedFields: ["name", "email", "message"]
  branch: "master"
  commitMessage: "Added Staticman comment data"
  filename: "comment-{@timestamp}"
  format: "yaml"
  moderation: true
  name: "Julian I. Kamil / etechcetera"
  path: "_data/comments/{options.slug}"
  requiredFields: ["name", "email", "message"]
  transforms:
    email: md5
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
